# nginx

```CMD
cd docker
docker build -t nginx22 .
docker run --rm -d -p 8080:80 --name nginx22 nginx22
docker exec -it <id-do-container> bash
nano

Com o docker-composer, vai instalar o portainer junto
cd docker
docker build -t nginx22 .
docker-compose up -d

```
