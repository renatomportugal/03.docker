version: '3.8'

services:
    
    konga:
        image: pantsel/konga
        container_name: konga
        ports:
            - "1337:1337"
        networks:
            - backend
        environment:
            - NODE_ENV=development
            - TOKEN_SECRET=ahjkscoenfudkjsiqoijoisoisjhysdr
    
    portainer:
        image: portainer/portainer-ce
        container_name: portainer
        ports:
            - "8000:8000"
            - "7000:9000"
        volumes:
            - /var/run/docker.sock:/var/run/docker.sock
            - portainer_data:/data
        networks:
            - backend

networks:
    backend:

volumes:
    portainer_data: